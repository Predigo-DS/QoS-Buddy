spring:
  application:
    name: qosentry-backend

  main:
    allow-circular-references: true

  datasource:
    url: jdbc:postgresql://localhost:5432/qosentry
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  data:
    redis:
      host: localhost
      port: 6379

springdoc:
  swagger-ui:
    path: /swagger-ui.html

server:
  port: ${SERVER_PORT:8080}

app:
  jwt:
    secret: CHANGE_ME_32_CHAR_SECRET_KEY_HERE
    expiration-ms: 86400000
  ai:
    anomaly-base-url: ${AI_ANOMALY_BASE_URL:http://localhost:8003}
    sla-base-url: ${AI_SLA_BASE_URL:http://localhost:8004}
